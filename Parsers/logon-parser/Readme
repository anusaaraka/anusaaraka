Install :
=======
1. Copy LOGON PARSER in $HOME folder

   cd $HOME
   svn checkout http://svn.emmtee.net/trunk logon

  Note:- Depending on the quality of your Internet connection , this initial download may take between ten minutes and a few hours.

   For more information see below link:-
   http://moin.delph-in.net/LogonInstallation

2. mkdir ~/logon_tmp

3. gedit ~/.bashrc
  
  LOGONROOT=~/logon/
  if [ -f ${LOGONROOT}/dot.bashrc ]; then
    . ${LOGONROOT}/dot.bashrc
  fi

   LOGONTMP=~/logon_tmp

Compile :
=======
1. source ~/.bashrc
2. cd $HOME_anu_test/
   sh shell_scripts/remove_out-files.sh 
   sh shell_scripts/anu_compile.sh

Run :
====
  sh Anusaaraka_logon.sh <input_file> 0 True

  Ex:- Anusaaraka_logon.sh verified 0 True
